/** @format */

// import the configuration file for the compiler

import config from './compiler.config.mjs';

const compileIcons = ({ config }) => {
	console.log({ config });
};

compileIcons({ config });
